# Supabase 연동 환경변수 — 실제 값은 .env(커밋 금지)에 넣으세요.
# Vercel 배포 시 Project Settings > Environment Variables 에 동일 키로 등록.
VITE_SUPABASE_URL=https://<your-project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-key>
